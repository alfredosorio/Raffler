<!--
<h1> <%= link_to 'Raffler', items_path %> </h1>
 Main Header and User Signed In Link
  <% if (user_signed_in?) && !(current_page?(new_profile_path)) %>
    <small>You are currently signed in as: <strong><%= current_user.profile.username %></strong></small>
  <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
<% end %>

<br />

<small>Become a seller today!</small>
<%= link_to 'Register', new_seller_path  %>

<br />
-->
<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'RAFFLER Online', root_path, class: "navbar-brand" %>
    </div>

<!-- Nav -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to 'Register', new_user_registration_path, class: "active" %><span class="sr-only">(current)</span></li>
        <li><%= link_to 'Login', new_user_session_path  %></li>
<!-- Admin Nav -->
        <% if (user_signed_in?) && current_user.id == 999 %>
          <li><%= link_to 'Bids', bids_path %></li>
        <% end %>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if (user_signed_in?) && !(current_page?(new_profile_path)) %>
          <li><%= link_to('Logout', destroy_user_session_path, :method => :delete) %></li>
        <% end %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Menu<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to 'Items', items_path %></li>
            <li><a href="#">Bids</a></li>
            <li role="separator" class="divider"></li>
            <li><a href="#">Your Seller Dashboard</a></li>
            <li><a href="#">Admin Dashboard</a></li>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
<% if (user_signed_in?) && !(current_page?(new_profile_path)) %>
  <small>You are currently signed in as: <strong><%= current_user.profile.first_name %></strong></small>
<% end %>

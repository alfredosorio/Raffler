<p id="notice"><%= notice %></p>

<h1>Bids Admin Console</h1>

<%= form_tag(items_path, method: :get) do %>
  <%= text_field_tag :query %>
  <%= submit_tag 'Search Item' %>
<% end %>

<table>
  <thead>
    <tr>
      <th>Bidder</th>
      <th>Item</th>
      <th>Seller</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bids.each do |bid| %>
      <tr>
        <td><%= bid.user.profile.username %></td>
        <td><%= bid.item.name %></td>
        <td><%= bid.seller.profile.username %></td>
        <td><%= link_to 'Draw Details', bid %></td>
        <td><%#= link_to 'Edit', edit_bid_path(bid) %></td>
        <td><%#= link_to 'Destroy', bid, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Bid', new_bid_path %>
